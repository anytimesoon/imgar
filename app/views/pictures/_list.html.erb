<% @pictures.each do |pic| %>
	<div>
		<h3><%= pic.title %></h3>
		<%= link_to (image_tag pic.path.url(:medium)), picture_path(pic) %>
		<p>
			Rating: <%= pic.rating.average %><br>
			Posted by: <%= link_to pic.user.name, user_path(pic.user) %>
		</p>
	</div>
<% end %>